{
  "name": "ihelfrich-hub",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run fetch-spokes && npm run pagefind && npm run clean-spokes",
    "build:fast": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "fetch-spokes": "node scripts/fetch-spokes.mjs",
    "pagefind": "pagefind --site dist --output-path dist/pagefind",
    "clean-spokes": "node scripts/clean-spokes.mjs"
  },
  "dependencies": {
    "astro": "^6.3.1"
  },
  "devDependencies": {
    "@astrojs/rss": "^4.0.18",
    "pagefind": "^1.5.2"
  }
}
