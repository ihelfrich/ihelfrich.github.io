import {calculateResidentOffer,forecastResidentValue} from './property-resident-value.mjs';
const costLabels={sellingFeePct:'Selling fee (%)',closingCosts:'Other closing costs ($)',repairs:'Repairs ($)',concessions:'Buyer credits ($)',holdingMonths:'Months until closing',monthlyHoldingCost:'Monthly carrying costs ($)'};
const escape=value=>String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
const money=value=>Number.isFinite(value)?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(value):'Not supplied';
function link(label,url){try{const u=new URL(url);if(!['http:','https:'].includes(u.protocol)||u.username||u.password)return '';return `<a href="${escape(u.href)}" target="_blank" rel="noopener noreferrer">${escape(label)}</a>`;}catch{return '';}}
function same(a,b){if(a===b)return true;if(!a||!b||typeof a!=='object'||typeof b!=='object'||Array.isArray(a)!==Array.isArray(b))return false;const keys=Object.keys(a);return keys.length===Object.keys(b).length&&keys.every(key=>Object.hasOwn(b,key)&&same(a[key],b[key]));}
function validate(data){if(data?.schema!=='resident-property-value-report-v1')throw Error('Unsupported resident report.');for(const[key,calculate]of [['offer',calculateResidentOffer],['forecast',forecastResidentValue]])if(data[key]){const result=calculate(data[key].assumptions);if(!same(data[key],result)||data.benchmark?.valueLow!==result.assumptions.valueLow||data.benchmark?.valueHigh!==result.assumptions.valueHigh)throw Error('Report calculation mismatch: recalculate with the current benchmark.');}}
export function residentValueCsv(data){validate(data);if(!data.offer)throw Error('Calculate a seller net sheet first.');
 const rows=[['Resident seller net sheet'],['Property',data.subject?.address||'Standalone scenario'],['Prepared',data.createdAt],['Range basis',data.benchmark.basis],['Range note',data.benchmark.note],['Scope','User-entered benchmark comparison; not an appraisal. Nominal USD before taxes.'],[],['Item','Lower benchmark','Upper benchmark','Buyer offer'],...data.offer.rows.map(r=>[r.label,r.marketLow,r.marketHigh,r.offer]),[],['Buyer price matching lower net',data.offer.breakEvenOfferPrice.low],['Buyer price matching upper net',data.offer.breakEvenOfferPrice.high],[],['Assumption','Market sale','Buyer offer'],...Object.keys(data.offer.assumptions.market).map(key=>[costLabels[key]||key,data.offer.assumptions.market[key],data.offer.assumptions.offer[key]])];
 const cell=v=>typeof v==='number'?String(v):`"${String(v??'').replace(/^\s*[=+@-]/,"'$&").replaceAll('"','""')}"`;return rows.map(row=>row.map(cell).join(',')).join('\r\n');
}
export function residentValueReport(data){validate(data);const e=escape,subject=data.subject,basis={'working-estimate':'User working estimate','independent-appraisal':'User-entered independent appraisal range','agent-comparables':'User-entered agent comparable-sales range'}[data.benchmark.basis]||'User-entered benchmark';
 const offer=data.offer,forecast=data.forecast,sales=data.saleEvidence,market=data.marketContext;
 const net=offer?`<section><h2>Seller’s pro forma</h2><p><strong>Buyer’s offer: ${money(offer.assumptions.offerPrice)}. Net proceeds: ${money(offer.offer.net)}.</strong></p><p>Net proceeds within the entered benchmark: ${money(offer.market.lowNet)} to ${money(offer.market.highNet)}.</p><table><thead><tr><th>Item</th><th>Lower benchmark</th><th>Upper benchmark</th><th>Buyer offer</th></tr></thead><tbody>${offer.rows.map(r=>`<tr><th>${e(r.label)}</th><td>${money(r.marketLow)}</td><td>${money(r.marketHigh)}</td><td>${money(r.offer)}</td></tr>`).join('')}</tbody></table><p>${Object.entries(offer.breakEvenOfferPrice).map(([key,value])=>value<=0?`Even a zero-price transfer at these cost terms meets the ${key==='low'?'lower':'upper'} benchmark’s modeled net proceeds (algebraic break-even ${money(value)}).`:`At the buyer’s cost terms, ${money(value)} matches the ${key==='low'?'lower':'upper'} benchmark’s net proceeds.`).join(' ')}</p>${offer.costs.market.low.totalSellingCostsUSD===0&&offer.costs.offer.totalSellingCostsUSD===0?'<p class="note">No selling costs were included: both routes have zero allowances. Review fees, repairs and carrying costs before relying on these net figures.</p>':''}<h3>Entered cost assumptions</h3><table><thead><tr><th>Assumption</th><th>Sale within benchmark</th><th>Buyer offer</th></tr></thead><tbody>${Object.keys(offer.assumptions.market).map(key=>`<tr><th>${e(costLabels[key]||key)}</th><td>${e(offer.assumptions.market[key])}</td><td>${e(offer.assumptions.offer[key])}</td></tr>`).join('')}</tbody></table>${offer.conventions.map(note=>`<p class="note">${e(note)}</p>`).join('')}</section>`:'<p>No seller net sheet was calculated.</p>';
 const paths=forecast?`<section><h2>Future-value scenarios</h2><p>What-if annual rates: lower ${e(forecast.assumptions.rates.lower)}%, middle ${e(forecast.assumptions.rates.steady)}%, higher ${e(forecast.assumptions.rates.higher)}%. No probability or calibrated forecast is implied.</p><table><thead><tr><th>Year</th><th>Lower path</th><th>Middle path</th><th>Higher path</th></tr></thead><tbody>${forecast.paths.lower.map((p,i)=>`<tr><th>${p.year}</th>${['lower','steady','higher'].map(key=>`<td>${money(forecast.paths[key][i].low)}–${money(forecast.paths[key][i].high)}</td>`).join('')}</tr>`).join('')}</tbody></table>${forecast.conventions.map(note=>`<p class="note">${e(note)}</p>`).join('')}</section>`:'';
 const comps=sales?`<section><h2>Nearby recorded-transfer evidence</h2><p>${e(sales.valuation?.reason||'No current valuation was produced.')}</p><p>Search radius: ${e(sales.coverage?.radiusKm)} km. Recorded dates: ${e(sales.coverage?.fromDate)} to ${e(sales.coverage?.toDate)}. Displayed ${sales.candidates?.length||0} of ${e(sales.totalCandidates??0)} candidate records. Coverage is ${sales.partial?'partial':'limited to the connected source snapshots'}.</p>${(sales.coverage?.warnings||[]).map(w=>`<p class="note">${e(w)}</p>`).join('')}<p>Administrative records are not independently verified comparable sales.</p><table><thead><tr><th>Property / source record</th><th>Recorded date</th><th>Price</th><th>Distance</th></tr></thead><tbody>${(sales.candidates||[]).map(c=>`<tr><th>${e(c.address||c.parcelId)}<small>${e(c.recordKey)}</small></th><td>${e(c.saleDateISO)}</td><td>${money(c.priceUSD)}</td><td>${Number(c.distanceKm).toFixed(2)} km</td></tr>`).join('')}</tbody></table><h3>Transaction details requiring review</h3>${(sales.candidates||[]).map(c=>`<p><strong>${e(c.recordKey)}</strong>. Price status: ${e(c.latestSalePriceStatus||'unknown')}. Sale validity code: ${e(c.latestSaleValidityCode||'unknown')}; market validity code: ${e(c.latestSaleMarketValidityCode||'unknown')}. Raw recorded date: ${e(c.latestSaleDateRaw||'not supplied')}. Two-digit year interpreted: ${c.latestSaleDateCenturyInferred==null?'unknown':c.latestSaleDateCenturyInferred?'yes':'no'}. Source ID: ${e(c.latestSaleSourceId||c.source?.id||'not supplied')}. ${link('Official transfer source',c.source?.url)}. Retrieved ${e(c.source?.retrievedAt||'not supplied')}.</p>`).join('')}<h3>Excluded records</h3>${Object.entries(sales.exclusionCounts||{}).map(([key,n])=>`<p>${e(key.replaceAll(/[-_]/g,' '))}: ${e(n)}</p>`).join('')}<h3>Source dates and references</h3>${(sales.sources||[]).map(s=>`<p>${e(s.name||s.source?.name||s.id)}. Latest observed sale date ${e(s.maxObservedSaleDateISO||'not established')}. Retrieved ${e(s.retrievedAt||s.source?.retrievedAt||'not supplied')}. Archive timestamp: ${e(s.archiveEntryTimestamp||'not supplied')}. ${link('Official source',s.url||s.source?.url)}</p>${(s.limitations||[]).map(note=>`<p class="note">${e(note)}</p>`).join('')}`).join('')}</section>`:'<p>No parcel sale evidence was loaded.</p>';
 const region=market?`<section><h2>Regional market context</h2><p>${e(market.source.seriesName)}. Latest observation ${e(market.latest.period)}; latest-year change ${Number.isFinite(market.changes.oneYearPct)?market.changes.oneYearPct.toFixed(2)+'%':'unavailable'}. Retrieved ${e(market.source.retrievedAt)}.</p><p>${link('FHFA series source',market.source.url)}. Regional change is not this property’s appreciation or a forecast.</p>${(market.limitations||[]).map(note=>`<p class="note">${e(note)}</p>`).join('')}</section>`:'';
 return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'"><title>Property evidence and offer report</title><style>body{max-width:960px;margin:40px auto;padding:0 26px;color:#18272d;font:15px/1.6 system-ui,sans-serif}h1{font-size:34px;line-height:1.15;letter-spacing:-.04em}h2{font-size:23px;margin-top:32px}h3{font-size:17px}table{width:100%;border-collapse:collapse;font-size:12px}th,td{text-align:left;border-bottom:1px solid #cbd3d7;padding:9px 6px;vertical-align:top}td{text-align:right;font-variant-numeric:tabular-nums}small{display:block;font-weight:400;overflow-wrap:anywhere}.note{font-size:12px;color:#455a64}section{margin-top:32px}a{color:#1e607d;overflow-wrap:anywhere}header{border-bottom:2px solid #273e48;padding-bottom:18px}@media print{body{margin:0;padding:0;font-size:11px}h1{font-size:25px}h2{break-after:avoid}tr{break-inside:avoid}thead{display:table-header-group}a{color:inherit}table{font-size:10px}@page{size:letter;margin:16mm}}</style></head><body><header><p>St. Louis City &amp; property lab · Ian Helfrich</p><h1>Property evidence &amp; offer report</h1><p>${e(subject?.address||'Standalone property scenario')}${subject?.parcelId?` · Parcel ${e(subject.parcelId)}`:''}</p><p class="note">Prepared ${e(data.createdAt)}. This report is not a professional appraisal or a validated current market estimate.</p></header><section><h2>Your value benchmark</h2><p>${money(data.benchmark.valueLow)} to ${money(data.benchmark.valueHigh)}. Basis: ${e(basis)}.</p>${data.benchmark.note?`<p>${e(data.benchmark.note)}</p>`:''}<p class="note">The site has not independently verified this range. Revisit the comparison if the range or cost assumptions change.</p></section>${net}${paths}${comps}${region}<section><h2>Scope and unresolved questions</h2>${data.limitations.map(note=>`<p>${e(note)}</p>`).join('')}<p>Document property condition, improvements, repair quotations, comparable-sale concessions and offer contingencies. Ask an independent local appraiser or real-estate professional to review evidence that materially changes your decision.</p></section></body></html>`;
}
